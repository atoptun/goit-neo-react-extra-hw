export const baseBox = { display: 'flex', alignItems: 'center', gap: 2 };

export const welcomeTypo = { display: { xs: 'none', sm: 'block' } };

export const userTypo = { fontWeight: 600, color: 'text.primary' };
